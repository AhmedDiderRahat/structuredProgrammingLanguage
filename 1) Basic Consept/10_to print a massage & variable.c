//write a program to print a massage  and variable
#include <stdio.h>

int main()
{
    int x, y;

    x = 5;
    y = x * 10;

    printf("x = %d \ny = %d \n%d \is the multiple of \n%d\n\n", x, y, y, x);

    getch();

    return 0;
}
